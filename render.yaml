services:
  - type: web
    name: gubernatorial-simulation-results-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 2 --threads 2 --timeout 60
    healthCheckPath: /health
